为了长时间不断开数据库连接，需要不停地mysql_ping, 而设置mysql_ping的时候需要及时获取；而为了不丢失连接，也必须在切换同一个连接不同数据库名的时候，需要getdatabase一下。否则会出现找不到 zhuaqu.dpps_project这样的错误。即数据库的混乱，因为是同一个host端口，等连接信息，除了数据库名不一样。所以会出现后来的数据库名覆盖最初的数据库名称的问题


